extends layout

block content
  h1= title
  button(onclick="location.href = '/new'" class="new-message" value="New Message") New Message
  div(class="message-board")
    each message in messages 
      div(class="message")
        div(class="message-header")
          div= message.user
          div= message.added
        div= message.text
